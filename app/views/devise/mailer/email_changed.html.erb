<p>Hallo <%= recipient_name(@resource) %>,</p>

<% if @resource.try(:unconfirmed_email?) %>
  <p>deine E-Mail-Adresse wird zu <%= @resource.unconfirmed_email %> geändert.</p>
<% else %>
  <p>deine E-Mail-Adresse wurde zu <%= @resource.email %> geändert.</p>
<% end %>
